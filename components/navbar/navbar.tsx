import { NextPage } from "next";

const Navbar: NextPage = () => {
  return (
    <nav className="container mx-auto py-2 px-1 blue">
      This is the nav bar
    </nav>
  )
};

export default Navbar;